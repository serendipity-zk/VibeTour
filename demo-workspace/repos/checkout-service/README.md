# Checkout Service

This folder represents a repository nested inside the outer VibeTour demo workspace.

Its lesson lives in `.code-lessons/walkthroughs/checkout-flow.yaml`, while every code location in that YAML is relative to this directory. Open the outer `demo-workspace` to verify that VibeTour discovers the lesson without adding this folder as a separate VS Code workspace root.
