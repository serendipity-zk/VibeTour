# VibeTour Workspace

This small multi-file workspace exists only to give the extension stable code ranges to annotate and cross-file links to navigate.

The lesson definition is discovered automatically from `.code-lessons/request-lifecycle.yaml`.

Use the **VibeTour** book icon in the Activity Bar, select a step, and try the inline actions.
